@model IEnumerable<MVC.Models.DTOs.ProductoDTOvista>

@{
    ViewData["Title"] = "Lista de productos";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>

    <!--=============== REMIXICONS ===============-->
    <link href="https:cdn.jsdelivr.net/npm/remixicon@3.2.0/fonts/remixicon.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css">

    <style>
        .tabla-container {
            max-height: 500px;
            overflow-y: auto;
            border: 1px solid #ddd;
            border-radius: 8px;
        }

        .tabla-empleados thead {
            position: sticky;
            top: 0;
            background-color: #f5f5f5;
            z-index: 1;
        }

        .tabla-container::-webkit-scrollbar {
            width: 8px;
        }

        .tabla-container::-webkit-scrollbar-thumb {
            background-color: #888;
            border-radius: 4px;
        }

            .tabla-container::-webkit-scrollbar-thumb:hover {
                background-color: #555;
            }
    </style>
</head>
<body>

    <main class="container full-width">
        <h2 class="titulo">Lista de Productos</h2>

        <div class="search-filter">
            <input type="text" placeholder="Buscar por ID o Nombre de producto" id="searchInput">
            <button class="btn yellow">Buscar</button>
            <a asp-controller="Productos" asp-action="crearProducto" class="btn green">Crear Producto</a>
        </div>

        <div class="tabla-container">
            <table class="tabla-empleados">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nombre producto</th>
                        <th>Proveedor</th>
                        <th>Categoría</th>
                        <th>Unidades</th>
                        <th>Precio costo</th>
@*                         <th>Precio venta</th>
 *@                     <th>Acción</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var producto in Model)
                    {
                        <tr>
                            <td>@producto.Id</td>
                            <td>@producto.Nombre</td>
                            <td>@producto.ProveedorNombre</td>
                            <td>@producto.CategoriaNombre</td>
                            <td>@producto.Stock</td>
                            <td>@producto.PrecioProducto</td>

                            <td class="acciones">
                                <a asp-controller="Productos" asp-action="modificarProducto" asp-route-id="@producto.Id"
                                   class="btn-icon yellow-outline" title="Editar">
                                    <i class="ri-pencil-line"></i>
                                </a>

                                <!-- FORMULARIO POST PARA ELIMINAR -->
                                <form asp-controller="Productos" asp-action="eliminarProducto" method="post" style="display:inline;">
                                    <input type="hidden" name="id" value="@producto.Id" />
                                    <a href="#" class="btn-icon yellow-outline" title="Eliminar" onclick="confirmDelete(this)">
                                        <i class="ri-delete-bin-line"></i>
                                    </a>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </main>

    <!-- Modal de Confirmación -->
    <div id="deleteModal" class="modal" style="display: none;">
        <div class="modal-content">
            <div class="modal-header">
                <i class="ri-alert-line" style="font-size: 2rem; color: #ff4d4d;"></i>
                <h3>Confirmar eliminación</h3>
            </div>
            <p>¿Estás seguro que deseas eliminar este producto?</p>
            <div class="modal-buttons">
                <button class="btn confirm" id="confirmBtn">Sí, eliminar</button>
                <button class="btn cancel" id="cancelBtn">Cancelar</button>
            </div>
        </div>
    </div>

    <script>
        function confirmDelete(element) {
            const modal = document.getElementById('deleteModal');
            modal.style.display = 'block';

            const productRow = element.closest('tr');
            const productId = productRow.querySelector('td').innerText;

            const confirmBtn = document.getElementById('confirmBtn');
            confirmBtn.onclick = function() {
                console.log('Eliminar producto con ID:', productId);
                modal.style.display = 'none';
            }

            const cancelBtn = document.getElementById('cancelBtn');
            cancelBtn.onclick = function() {
                modal.style.display = 'none';
            }
        }

        window.onclick = function(event) {
            const modal = document.getElementById('deleteModal');
            if (event.target == modal) {
                modal.style.display = 'none';
            }
        }
    </script>

</body>
</html>
